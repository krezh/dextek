---
# yaml-language-server: $schema=https://kubernetes-schemas.plexuz.xyz/kopiur.home-operations.com/restore_v1alpha1.json
apiVersion: kopiur.home-operations.com/v1alpha1
kind: Restore
metadata:
  name: ${APP}
spec:
  failurePolicy:
    podStartupDeadlineSeconds: 1800
  mover:
    securityContext:
      runAsUser: 0
      runAsNonRoot: false
      capabilities:
        add:
          - DAC_OVERRIDE
          - CHOWN
          - FOWNER
        drop:
          - ALL
    privilegedMode: true # preserves original UID/GID ownership on restore
  options:
    parallel: 20
  policy:
    onMissingSnapshot: Continue
  source:
    fromPolicy:
      name: ${APP}
      offset: 0
  target:
    populator: {}
