---
# yaml-language-server: $schema=https://kubernetes-schemas.plexuz.xyz/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: ${APP}-db
  namespace: &namespace databases
spec:
  targetNamespace: *namespace
  dependsOn:
    - name: external-secrets-stores
      namespace: flux-system
    - name: cnpg-cluster
      namespace: flux-system
  path: ./clusters/shared/components/cnpg/pass/database
  prune: true
  sourceRef:
    kind: OCIRepository
    name: flux-system
    namespace: flux-system
  interval: 1h
  retryInterval: 2m
  timeout: 5m
  postBuild:
    substitute:
      APP: ${APP}
      OWNER: ${DATABASE_OWNER:=${APP}}
      EXTENSIONS: ${q:='}${DATABASE_EXTENSIONS:=}${q:='}
      DATABASE_SUPERUSER: ${q:='}${DATABASE_SUPERUSER:=false}${q:='}
      emptyArray: "[]"
  healthChecks:
    - apiVersion: postgresql.cnpg.io/v1
      kind: Database
      name: ${APP}
      namespace: *namespace
