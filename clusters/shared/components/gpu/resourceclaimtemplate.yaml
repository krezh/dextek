---
apiVersion: resource.k8s.io/v1 # yayamlls-disable-line
kind: ResourceClaimTemplate # yayamlls-disable-line
metadata: # yayamlls-disable-line
  name: ${APP}-gpu
spec:
  spec:
    devices:
      requests:
        - name: gpu
          exactly:
            deviceClassName: gpu.intel.com
            adminAccess: true
            allocationMode: All
