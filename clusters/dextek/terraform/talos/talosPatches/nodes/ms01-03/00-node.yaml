machine:
  type: controlplane
  install:
    disk: /dev/sda
    wipe: false
    diskSelector:
      model: "Samsung SSD*"
  nodeLabels:
    node.kubernetes.io/gpu: "true"
    topology.kubernetes.io/region: main
