---
# yaml-language-server: $schema=https://kubernetes-schemas.plexuz.xyz/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app profilarr
  namespace: flux-system
spec:
  targetNamespace: downloads
  dependsOn:
    - name: envoy-gateway
  path: ./clusters/dextek/apps/downloads/profilarr/app
  prune: true
  sourceRef:
    kind: OCIRepository
    name: flux-system
  wait: false
  interval: 1h
  timeout: 5m
  components:
    - ../../../../../shared/components/kopiur/backup
  postBuild:
    substitute:
      APP: *app
      KOPIUR_CAPACITY: 10Gi
      EXT_AUTH_OUTPOST: external
      EXT_AUTH_TARGET: *app
