---
# yaml-language-server: $schema=https://raw.githubusercontent.com/fluxcd-community/flux2-schemas/main/kustomization-kustomize-v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app dashbrr
  namespace: flux-system
spec:
  targetNamespace: downloads
  commonMetadata:
    labels:
      app.kubernetes.io/name: *app
  dependsOn:
    - name: volsync
    - name: cloudnative-pg-cluster
  interval: 30m
  retryInterval: 1m
  timeout: 5m
  path: ./clusters/dextek/apps/downloads/dashbrr/app
  prune: true
  wait: true
  sourceRef:
    kind: GitRepository
    name: dextek
  postBuild:
    substitute:
      APP: *app
      APP_UID: "568"
      APP_GID: "568"
