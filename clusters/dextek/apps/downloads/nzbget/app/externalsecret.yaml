---
# yaml-language-server: $schema=https://kubernetes-schemas.plexuz.xyz/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: nzbget
  namespace: downloads
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: infisical
  target:
    name: nzbget-secret
    creationPolicy: Owner
    template:
      data:
        APIKEY: "{{ .NZBGET_API_KEY }}"
  dataFrom:
    - extract:
        key: /Kubernetes/DexTek/NZBGET
