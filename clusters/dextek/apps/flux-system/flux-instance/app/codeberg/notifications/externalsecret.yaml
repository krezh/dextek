---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: codeberg-token
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: infisical
  target:
    name: codeberg-token
    template:
      data:
        token: "{{ .FLUX_CODEBERG_TOKEN }}"
  data:
    - secretKey: FLUX_CODEBERG_TOKEN
      remoteRef:
        key: /Kubernetes/DexTek/Flux/FLUX_CODEBERG_TOKEN
