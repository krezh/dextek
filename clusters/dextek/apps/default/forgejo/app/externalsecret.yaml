---
# yaml-language-server: $schema=https://kubernetes-schemas.plexuz.xyz/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: forgejo
  namespace: default
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: infisical
  target:
    name: forgejo-secret
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        CF_TURNSTILE_SITEKEY: "{{ .FORGEJO_TURNSTILE_SITE_KEY }}"
        CF_TURNSTILE_SECRET: "{{ .FORGEJO_TURNSTILE_SECRET_KEY }}"
  dataFrom:
    - extract:
        key: /Kubernetes/DexTek/FORGEJO
---
# yaml-language-server: $schema=https://kubernetes-schemas.plexuz.xyz/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: forgejo-db
  namespace: default
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: crunchy-pgo-secrets
  target:
    name: forgejo-db-secret
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        FORGEJO__database__HOST: '{{ index . "host" }}'
        FORGEJO__database__NAME: '{{ index . "dbname" }}'
        FORGEJO__database__USER: '{{ index . "user" }}'
        FORGEJO__database__PASSWD: '{{ index . "password" }}'
  dataFrom:
    - extract:
        key: postgres-pguser-forgejo
---
# yaml-language-server: $schema=https://kubernetes-schemas.plexuz.xyz/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: forgejo-oauth
  namespace: default
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: infisical
  target:
    name: forgejo-oauth-secret
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        key: "{{ .FORGEJO_OAUTH_CLIENT_ID }}"
        secret: "{{ .FORGEJO_OAUTH_CLIENT_SECRET }}"
  dataFrom:
    - extract:
        key: /Kubernetes/DexTek/FORGEJO
