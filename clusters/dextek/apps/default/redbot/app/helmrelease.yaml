---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: redbot
  namespace: default
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: redbot
  values:
    controllers:
      app:
        type: statefulset
        replicas: 1
        strategy: RollingUpdate
        containers:
          app:
            image:
              repository: phasecorex/red-discordbot
              tag: extra-audio-py311@sha256:204308d88d0cf7f3ce033e1f10c68836cbe46bad2de3f77342ec4a333854cac1
            env:
              PUID: 568
              PGID: 568
            envFrom:
              - secretRef:
                  name: redbot-secret
            resources:
              requests:
                cpu: 50m
                memory: 400Mi
              limits:
                memory: 1200Mi
    persistence:
      config:
        existingClaim: redbot-config
        globalMounts:
          - path: /data
