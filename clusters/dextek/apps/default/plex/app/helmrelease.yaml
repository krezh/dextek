---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app plex
  namespace: default
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: plex
  values:
    defaultPodOptions:
      securityContext:
        runAsUser: 568
        runAsGroup: 568
        fsGroup: 568
        fsGroupChangePolicy: "OnRootMismatch"
        supplementalGroups: [44]
      resourceClaims:
        - name: gpu
          resourceClaimTemplateName: plex-gpu
    controllers:
      app:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: ghcr.io/home-operations/plex
              tag: 1.43.1.10611@sha256:a9c3723cb31cc9621df27cf5801184c290293adf1df2bfaea4b8b6fc01dbfd96
            env:
              PLEX_ADVERTISE_URL: https://plex-lb.talos.plexuz.xyz:32400
            envFrom:
              - secretRef:
                  name: plex-secret
            resources:
              claims:
                - name: gpu
              requests:
                cpu: 100m
                memory: 2Gi
              limits:
                memory: 4Gi
    service:
      app:
        controller: app
        type: LoadBalancer
        annotations:
          external-dns.alpha.kubernetes.io/hostname: plex-lb.talos.plexuz.xyz
          lbipam.cilium.io/ips: 192.168.25.16
        externalTrafficPolicy: Local
        ports:
          https:
            protocol: HTTPS
            appProtocol: https
            port: &port 32400
    ingress:
      app:
        enabled: true
        className: nginx-internal
        annotations:
          nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
        hosts:
          - host: &hostlocal "{{ .Release.Name }}.talos.plexuz.xyz"
            paths:
              - path: /
                pathType: Prefix
                service:
                  identifier: app
                  port: *port
        tls:
          - hosts:
              - *hostlocal
    persistence:
      config:
        enabled: true
        existingClaim: plex-config
        globalMounts:
          - path: /config
      media:
        type: nfs
        server: jotunheim.fast.internal
        path: /mnt/tank/media
        globalMounts:
          - path: /media
      local:
        enabled: true
        type: emptyDir
        medium: Memory
        globalMounts:
          - path: /.local
