---
# yaml-language-server: $schema=https://kubernetes-schemas.plexuz.xyz/kopiur.home-operations.com/clusterrepository_v1alpha1.json
apiVersion: kopiur.home-operations.com/v1alpha1
kind: ClusterRepository
metadata:
  name: tank
spec:
  allowedNamespaces:
    all: true
  backend:
    filesystem:
      path: /repo
      volume:
        nfs:
          server: jotunheim.fast.internal
          path: /mnt/tank/kopia
  encryption:
    passwordSecretRef:
      name: kopiur-repository-secret
      namespace: kopiur-system
      key: KOPIA_PASSWORD
  maintenance:
    enabled: true
    namespace: kopiur-system
    schedule:
      quick:
        cron: "0 */6 * * *"
        jitter: 30m
      full:
        cron: "0 3 * * 0"
        jitter: 1h
      timezone: UTC
  scheduleDefaults:
    timezone: Europe/Stockholm
