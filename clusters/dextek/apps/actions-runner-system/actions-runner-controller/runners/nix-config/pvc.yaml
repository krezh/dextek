---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nix-store
  namespace: actions-runner-system
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ceph-filesystem
  resources:
    requests:
      storage: 500Gi
